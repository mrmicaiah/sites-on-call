---
layout: layouts/base.njk
title: "Articles — Web Design & SEO Tips for Local Businesses"
description: "Tips, guides, and insights about web design, SEO, and content marketing for local service businesses."
---

<section class="page-hero">
  <div class="section-inner">
    <div class="section-label">Resources</div>
    <h1 class="section-title">Articles</h1>
    <p class="section-subtitle" style="margin:0 auto;">Tips, guides, and insights to help your local business grow online.</p>
  </div>
</section>

<section>
  <div class="section-inner">
    {% if articles.length > 0 %}
      <div class="articles-grid">
        {% for article in articles %}
          <article class="article-card">
            <a href="{{ article.url }}">
              {% if article.image %}
                <img src="{{ article.image }}" alt="{{ article.title }}" class="article-card-image" loading="lazy">
              {% else %}
                <div class="article-card-image" style="background:var(--cream);display:flex;align-items:center;justify-content:center;">
                  <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="var(--muted)" stroke-width="1.5"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
                </div>
              {% endif %}
              <div class="article-card-content">
                <div class="article-card-date">{{ article.publishedAt | readableDate }}</div>
                <h2 class="article-card-title">{{ article.title }}</h2>
                <p class="article-card-excerpt">{{ article.excerpt | excerpt }}</p>
              </div>
            </a>
          </article>
        {% endfor %}
      </div>
    {% else %}
      <div class="empty-state">
        <h3>Coming Soon</h3>
        <p>We're working on helpful articles about web design, SEO, and growing your local business online. Check back soon!</p>
      </div>
    {% endif %}
  </div>
</section>

<section class="final-cta">
  <div class="section-inner">
    <div class="section-label">Ready to grow?</div>
    <h2>Get your free website today</h2>
    <p>Tell us about your business and we'll get started on your custom site.</p>
    <button class="btn-primary" onclick="openContactModal()">
      Let's Talk 
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
    </button>
  </div>
</section>